@using X.PagedList
@using X.PagedList.Mvc.Core
@using X.PagedList.Web.Common

@model IPagedList<Banding.Core.Models.Entities.MySql.Provincia>
@{
    ViewData["Title"] = "Index";
}

<div class="container-xxl hero-header" style="padding-bottom:0px">
    <div class="contenedor-grande" style="margin: 20px 100px;padding: 40px;">


        <h1>Provincias</h1>

        <p>
            <a class="btn btn-dark" asp-action="Create">Crear</a>
        </p>
        <table class="table">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.First().NombreProvincia)
                    </th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
        @foreach (var item in Model) {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.NombreProvincia)
                    </td>
                    <td>
                        <a class="btn btn-dark" asp-action="Edit" asp-route-id="@item.IdProvincia">Editar</a> |
                        <a class="btn btn-dark" asp-action="Details" asp-route-id="@item.IdProvincia">Detalles</a> |
                        <a class="btn btn-dark" asp-action="Delete" asp-route-id="@item.IdProvincia">Eliminar</a>
                    </td>
                </tr>
        }
            </tbody>
        </table>

        @Html.PagedListPager((IPagedList)Model, page => 
            Url.Action("Index", new { page = page }), 
            new PagedListRenderOptions {
                LiElementClasses = new string[] { "page-item" },
                PageClasses = new string[] { "page-link" }
        })

    </div>
</div>

<partial name="~/Views/Shared/_NavbarStylePartial.cshtml" />
